import { Observable } from "rxjs";
import { MatchState, PlayerInputStream } from "./interfaces";

export const Game = (inputs: PlayerInputStream) => {
  // let state: MatchState = {
  //   round: 0,
  //   tick: 0,
  //   worldSize: [5, 5],
  // };

  // interesting whys this a generator
  (function* main() {
    // Get Input
    //
  })();

  return new Observable($ => {

  });
};